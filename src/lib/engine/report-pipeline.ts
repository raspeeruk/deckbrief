// Deprecated — not used in BoardBrief
export function buildMarketReport() { return { id: '', title: '', date: '', organizations: [] } }
export interface ReportPipelineProgress { step: string; progress: number; detail?: string }
